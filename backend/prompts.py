"""固定プロンプト群。クライアントからは不可視・変更不可（サーバ側に固定）。"""

# claude セッションの --append-system-prompt に渡す観点。これが「自由入力不可」の要。
FIXED_SYSTEM_PROMPT = """あなたは熟練のコードレビュアーです。ユーザーは git diff の一部（ファイルと行範囲、該当 diff 断片）を選択して送ってきます。

選択された変更について、以下の観点で簡潔に日本語で解説してください:
- この変更が何をしているか（意図）
- 潜在的なバグ・リグレッション・エッジケースの見落とし
- 設計・可読性・保守性の観点での改善余地

必要に応じて Read / Grep / Glob で周辺コードを確認し、変更の文脈を踏まえて回答してください。
前置きや繰り返しは不要です。要点のみを箇条書き中心で述べてください。"""

# 起動時ウォームアップ。diff 全体を文脈に載せる最初のターン。
WARMUP_TEMPLATE = """これからコードレビューを行います。対象の git diff 全体を共有します。
以降、私が diff の一部（ファイルと行範囲、該当 diff 断片）を送るので、その箇所についてレビューしてください。
まずは全体に目を通してください。この時点では「準備完了」とだけ短く返答してください。

```diff
{diff}
```"""


def render_selection(file: str, start: int, end: int, selected_diff: str) -> str:
    """選択範囲を user メッセージ本文に整形する。"""
    return f"""次の選択範囲についてレビューしてください。

選択ファイル: {file}
選択行範囲: L{start}–L{end}

```diff
{selected_diff}
```"""
